<div class="accordion-item">
  <div class="accordion-header" id="heading2">
    {{#if acc-open}}
    <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#{{collapse-id}}"
      aria-expanded="true" aria-controls="{{collapse-id}}" aria-label="Mostra tutto o nascondi">
      <span class="d-flex align-items-center">
      Mostra tutto
        <svg class="icon icon-primary icon-sm">
          <use href="./assets/bootstrap-italia/dist/svg/sprite.svg#it-expand"></use>
        </svg>
      </span>
    </button>
    {{else}}
    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#{{collapse-id}}"
      aria-expanded="false" aria-controls="{{collapse-id}}" aria-label="Mostra tutto o nascondi">
      <span class="d-flex align-items-center">
        Mostra tutto
        <svg class="icon icon-primary icon-sm">
          <use href="./assets/bootstrap-italia/dist/svg/sprite.svg#it-expand"></use>
        </svg>
      </span>
    </button>
    {{/if}}
  </div>
  <div id="{{collapse-id}}" class="accordion-collapse collapse rounded{{#if acc-open}} show{{/if}}" role="region">
    <div class="accordion-body p-0">
      {{#if isee-success}}
      {{>cmp-info-summary/cmp-info-summary info-title="Dati ISEE" info=true
      info-list=servizi-pagamento.controllare-dati-personali.isee-success class="border border-light p-3 p-lg-4"
      }}
      {{else}}
      {{>cmp-info-summary/cmp-info-summary info-title="Dati ISEE" info=true
      info-list=iscrizione-graduatoria.controllare-dati-personali.isee class="border border-light p-3 p-lg-4"
      btn=warning warning-id="isee-info"}}
      {{/if}}
    </div>
  </div>
</div>