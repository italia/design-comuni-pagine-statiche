{{#if no-link}}
<div class="chip chip-simple {{class}}" href="#" data-element="service-topic">
  <span class="chip-label">{{label-tag}}</span>
</div>
{{else}}
<a class="chip chip-simple {{class}}" href="#" data-element="service-topic">
  <span class="chip-label">{{label-tag}}</span>
</a>
{{/if}}